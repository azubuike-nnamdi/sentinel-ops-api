/**
 * Shared cross-cutting contracts and helpers used by feature modules.
 * Prefer `common/` for infrastructure (filters, guards decorators, pipes).
 */
export { bootstrapTelemetry } from './otel-bootstrap';
